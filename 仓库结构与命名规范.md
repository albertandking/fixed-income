# 仓库结构与命名规范

对齐 [financial-data-science](https://github.com/albertandking/financial-data-science) 的工程化约定，便于复用同一套写作 / 构建 / 在线运行流程。

## 一、目录结构

```
fixed-income/
├── pyproject.toml            # uv 项目与依赖定义
├── uv.lock                   # 锁定版本（入库，保证复现）
├── .python-version           # Python 版本（3.14，兼容 3.11+）
├── mkdocs.yml                # 成书配置与章节导航（见本目录 mkdocs.yml）
├── README.md
│
├── book/                     # 【正文】MkDocs 文档源（Markdown）
│   ├── index.md              # 首页
│   ├── preface.md            # 前言
│   ├── assets/figures/       # 正文图示（PNG，由 scripts/make_figures.py 生成）
│   ├── part1/ … part8/       # 八个部分，一章一个 NN-题目.md
│   └── appendix/             # 附录 A–E
│
├── notebooks/                # 【代码】各章可执行 Jupyter Notebook
│   └── chNN_主题.ipynb        # 顶部含 Colab/Binder 徽章 + 自举单元
│
├── src/fi/                   # 全书复用工具包（import fi）
│   ├── data.py               # 数据读取 / 内置数据集加载
│   ├── cashflow.py           # 现金流、计息惯例、应计利息
│   ├── pricing.py            # 定价、YTM、即期/远期
│   ├── curve.py              # 期限结构 bootstrap 与插值
│   ├── risk.py               # 久期、凸性、DV01、KRD
│   ├── credit.py             # Merton/KMV、约化模型
│   ├── backtest.py           # 策略回测框架
│   └── plotting.py           # 统一中文绘图样式
│
├── data/
│   ├── raw/                  # 原始/下载数据（不入库）
│   ├── processed/            # 清洗好的内置示例数据（入库，离线可跑）
│   └── README.md
│
├── scripts/
│   ├── make_sample_data.py   # 离线生成内置示例数据集
│   ├── fetch_data.py         # 从 akshare/tushare 抓取真实数据
│   ├── make_figures.py       # 生成正文静态图
│   └── export_notebooks.py   # notebook -> markdown 片段手动导出
│
├── binder/                   # Binder 环境配置
├── .github/workflows/        # CI：push main → 构建 MkDocs → 发布 Pages
└── tests/                    # 冒烟测试，保证代码可跑
```

## 二、写作约定

- **一章 = 一个 `book/partX/NN-题目.md` + 一个 `notebooks/chNN_主题.ipynb`**（编号严格对应，见大纲总览表）。
- 正文展示代码/图表时**引用 notebook 导出的片段**，避免代码两处维护。
- 所有 notebook 默认依赖 `data/processed/` 内置示例数据，**断网也能跑**；用到联网接口（akshare/tushare）的代码放在明确标注的「联网」小节。
- 复用逻辑（计息、定价、曲线、风险、信用、回测、绘图）一律抽进 `src/fi/`，正文与 notebook 都 `from fi import ...`。
- 每章正文统一结构：`学习目标(abstract) → 直觉与推导 → 例X.X 算例 → 中国市场案例 → 习题与编程实验 → 本章小结`。
- 数学用 `pymdownx.arithmatex`（`$...$` / `$$...$$`）；提示框用 admonition（`!!! tip/warning/note/example/abstract`）。
- 图编号「图 N-M」，例题编号「例 N.M」，与参考书一致。

## 三、命名规范

| 对象 | 规则 | 示例 |
|---|---|---|
| 正文文件 | `partX/NN-kebab-英文.md` | `part4/08-money-market-repo.md` |
| Notebook | `chNN_snake_英文.ipynb` | `ch08_money_market_repo.ipynb` |
| 复用模块 | `src/fi/snake.py` | `src/fi/curve.py` |
| 图文件 | `chNN_描述.png` | `ch06_key_rate_duration.png` |
| 内置数据 | `data/processed/描述.csv/parquet` | `cgb_yield_curve.parquet` |

## 四、工具链与可复现

- 依赖用 `uv` 管理；`uv sync` 一键装好，`uv.lock` 锁定版本入库。
- 可选依赖分组：`--extra data`（akshare/tushare 联网）、`--extra quantlib`（QuantLib）。
- 每章 notebook 顶部放 Colab + Binder 徽章；Colab 自举单元自动 clone 仓库并 `pip install -e .`，本地运行时自动跳过。
- 数据优先级：**内置离线样本（默认）> akshare/tushare（免费联网）> Wind/WindPy（仅在附录注明"如有授权可选"）**。

## 五、迁移清单（从 v1 到本结构）

- [ ] 用 `大纲-v2.md` 替换原 docx 大纲，建立 `book/` 与 `notebooks/` 空骨架。
- [ ] 把 v1 中 WindPy 演示改写为 akshare/tushare + 内置样本。
- [ ] 重写 `preface.md`（对准固定收益，非量化交易）。
- [ ] 替换参考文献为固收文献（见大纲末）。
- [ ] 为每章补「学习目标」与「习题与编程实验」两节。
